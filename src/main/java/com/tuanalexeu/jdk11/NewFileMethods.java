package com.tuanalexeu.jdk11;

/**
 * Additionally, it's now easier to read and write Strings from files.
 * We can use the new readString and writeString static methods from the Files class
 */
public class NewFileMethods {

    public static void readString() {
        // String testString = Files.readString(tempDir);
    }

    public static void writeString() {
        // Path filePath = new Files.writeString(Files.createTempFile(tempDir, "test", ".txt"), "Sample text");

    }
}
